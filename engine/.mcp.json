{
  "mcpServers": {
    "panelalpha-engine": {
      "type": "http",
      "url": "${user_config.server_url}",
      "headers": {
        "Authorization": "Bearer ${user_config.api_token}"
      }
    }
  }
}
